<UserControl x:Class="AdministrationApp.MVVM.Views.WeatherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <Grid Margin="0 10 0 0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="Temperature" Foreground="White" FontSize="14"
                   FontWeight="SemiBold" />
        <TextBlock Grid.Column="1" Text="Humidity" Foreground="White" FontSize="14"
                   FontWeight="SemiBold" />
        <TextBlock Grid.Column="0" Margin="0 10 0 0" Text="{Binding Temperature, StringFormat='\{0}°C'}"
                   Foreground="White" FontSize="32"
                   FontWeight="SemiBold" />
        <TextBlock Grid.Column="1" Margin="0 10 0 0" Text="{Binding Humidity, StringFormat='\{0}%'}" Foreground="White"
                   FontSize="32"
                   FontWeight="SemiBold" />

    </Grid>
</UserControl>